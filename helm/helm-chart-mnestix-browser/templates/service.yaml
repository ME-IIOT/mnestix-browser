apiVersion: v1
kind: Service
metadata:
  name: {{ include "mnestix-frontend.fullname" . }}
  labels:
    app: {{ include "mnestix-frontend.name" . }}
    chart: {{ include "mnestix-frontend.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: {{ include "mnestix-frontend.name" . }}
    release: {{ .Release.Name }}
